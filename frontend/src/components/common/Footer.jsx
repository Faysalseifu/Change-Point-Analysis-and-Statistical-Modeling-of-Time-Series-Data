import React from "react";

const Footer = () => (
	<footer className="footer">
		Dashboard built with Flask + React + Recharts. Data sources: processed Brent
		prices and curated event timeline (2012-2022).
	</footer>
);

export default Footer;
